# approval
name: approval
description: 'Manual approveal'

runs:
  using: "composite"
  steps:
    - name: Set Greeting
      shell: bash
      environment: review
      run: echo "Approved by ${{ github.actor }}" # todo: this was supposed to use a composite action
